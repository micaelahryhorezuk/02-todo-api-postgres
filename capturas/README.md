# Capturas de evidencia

Guardá en esta carpeta las capturas PNG de Postman y Docker:

- `postman-resultados.png`: pantalla del Run con GET 200, POST 201, GET por ID 200, PUT 200 y DELETE 204.
- `postman-404.png`: pantalla del caso `GET /tasks/999999` con respuesta 404.
- `postman-delete-204.png`: pantalla individual de DELETE con `204 No Content` y tests `2/2`.
- `docker-compose-up.png`: consola con `docker compose up --build` sin errores.

Las imágenes se enlazan desde `ENTREGA.md` cuando estén guardadas con esos nombres.
